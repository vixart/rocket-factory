env:
  app_env: "development"
  service_name: "iam-service"
  service_version: "1.0.0"

grpc:
  host: "localhost"
  port: "50053"

logger:
  level: "info"

pg:
  host: "localhost"
  port: "55434"
  database: "iam-service"
  user: "iam-service-user"
  password: "iam-service-password"
  sslmode: "disable"

redis:
  host: "localhost"
  # Совпадает с хостовым портом redis-iam из iam.env (6380, не 6379 из-за redis-ratelimit).
  port: "6380"
  connection_timeout: "10s"
  max_idle: 10
  idle_timeout: "10s"
  cache_ttl: "1h"

session:
  ttl: "24h"

otel:
  endpoint: "localhost:4317"
  enable_otlp: true
  sampling_ratio: 1.0
