# Переопределения конфигурации для ЛОКАЛЬНОГО запуска сервиса.
# Файл читает сам сервис (godotenv в cmd/main.go, относительно рабочей директории),
# а cleanenv ставит env выше YAML — значения отсюда перебивают payment/config.local.yaml.
#
# Контейнерный аналог — payment/payment.docker.env.
#
# Окружение: docker, development, staging, production
APP_ENV=development

ENABLE_OTLP=true

GRPC_HOST=localhost
GRPC_PORT=50052

CONFIG_PATH=config.local.yaml
