env:
  app_env: "production"
  service_name: "inventory-service"
  service_version: "1.0.0"

grpc:
  host: "0.0.0.0"
  port: "50051"

logger:
  level: "info"

pg:
  host: "postgres-production"
  port: "55433"
  database: "demo"
  user: "MUST_BE_SET_VIA_ENV"
  password: "MUST_BE_SET_VIA_ENV"
  sslmode: "require"

client:
  iam_address: "0.0.0.0:50053"

otel:
  endpoint: "localhost:4317"
  enable_otlp: true
  sampling_ratio: 1.0
